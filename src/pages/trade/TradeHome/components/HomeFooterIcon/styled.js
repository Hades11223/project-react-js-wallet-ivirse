import styled from "styled-components";

export const GlobalIconsWrapper = styled.div`
    a {
        display: inline-block;
        padding-right: 12px;
    }
    a:last-child {
    position: relative;
    top: -4px;
  }
`